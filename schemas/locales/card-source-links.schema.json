{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:madoguchi-mimamori:schema:locale:card-source-links:1.0.0",
  "title": "Locale カードと案内先の関連",
  "description": "日本語と英語で共有するカードと案内先の関連表示文言のSchemaです。",
  "type": "object",
  "properties": {
    "schema_version": {
      "type": "string",
      "const": "1.0.0"
    },
    "data_updated_on": {
      "type": "string",
      "format": "date"
    },
    "items": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "pattern": "^card-source-[a-z0-9]+(?:-[a-z0-9]+)*$"
          },
          "button_label": {
            "type": "string",
            "minLength": 1
          },
          "public_note": {
            "type": "string",
            "minLength": 1
          },
          "locale_status": {
            "type": "string",
            "enum": ["draft", "under-review", "published", "needs-update", "archived"]
          },
          "content_revision": {
            "type": "integer",
            "minimum": 1
          },
          "based_on_ja_revision": {
            "type": "integer",
            "minimum": 1
          },
          "content_reviewed_on": {
            "type": "string",
            "format": "date"
          }
        },
        "required": ["id", "locale_status", "content_revision"],
        "if": {
          "properties": {
            "locale_status": {
              "const": "published"
            }
          },
          "required": ["locale_status"]
        },
        "then": {
          "properties": {
            "button_label": true,
            "content_reviewed_on": true
          },
          "required": ["button_label", "content_reviewed_on"]
        },
        "additionalProperties": false
      }
    }
  },
  "required": ["schema_version", "data_updated_on", "items"],
  "additionalProperties": false
}
