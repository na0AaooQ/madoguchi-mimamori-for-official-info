# AGENTS.md

この文書は、Codexを含む開発エージェントが「まどぐちみまもり」リポジトリで作業する際の実務ルールです。ユーザーの明示的な依頼と、本リポジトリの設計文書を優先してください。

## プロジェクトの目的

> 災害時などに、熊本県内の状況を確認したい方や、地域外から支援したい方が、確認したい情報を担当する公的機関・関係団体を見つけ、その団体自身の公式発表へ速やかに到達できるよう支援する。

本サイトは行政公式サイト、速報サイト、真偽判定サービスではありません。公的機関・関係団体自身の公式情報へ進むための案内サイトです。

## 現在の公開状態

第一版productionは2026年8月4日に公開済みです。正式URLは <https://madoguchi.kokoromimamori.na0aaooq.com/> で、日本語・英語、2分野、3カード、14案内先を提供しています。GitHub Pages、カスタムドメイン、HTTPS、`workflow_dispatch`だけの手動デプロイworkflowを使用します。

公開後の改善、継続運用、将来構想は[公開後バックログ](docs/POST_LAUNCH_BACKLOG.md)を参照してください。バックログ項目は実装済み・公開済みとは扱いません。

## 最優先原則

1. 利用者の安全、プライバシー、誤認防止を優先する。
2. 情報量や速報性より、分かりやすさと継続的な維持可能性を優先する。
3. 公式発表を転載せず、公式情報への入口を案内する。
4. 公式性は所定の根拠を人が確認し、根拠と確認日を記録する。
5. 確定事項、暫定事項、未確定事項を区別し、未確定事項を断定しない。
6. 依頼された範囲を超えて仕様や対象地域を拡張しない。

## 作業開始前の確認

作業開始時に、対象リポジトリで次を確認する。

```sh
pwd
git branch --show-current
git status -s
git diff origin/main
ls -la
```

あわせて、`README.md`、`AGENTS.md`、関連する`docs/`文書、`LICENSE`、`.gitignore`を読む。指定ブランチと異なる場合、または作業前から未コミット差分がある場合は、既存差分を修正・削除せず、ユーザーへ状況を報告する。

## 仕様・情報の扱い

- 勝手に機能、対象地域、掲載団体、情報量、データ項目を増やさない。
- 現在確定しているGitHub Pages、正式URL、URLパス、日英構成、データ構造、プライバシー説明は関連文書に従う。将来候補を決定済みとして書かず、明示依頼なしにホスティング、URL、アクセス解析、Cookieの仕様を変更しない。
- 停電、断水、火災、避難、運休、遅延、通行止め、診療、支援制度などの現在状況を転載しない。
- Web検索、クローリング、スクレイピング、RSS、外部API、AIなどによる自動収集・要約・判断機能を追加しない。
- 非公式情報、目撃情報、出所や公式性を確認できない情報を追加しない。
- 情報の真偽判定、団体の信頼度採点、ランキング、推奨順位付けを行わない。
- 掲載順を評価や推奨順位と誤認させない。
- 「最新」「リアルタイム」「安全を保証」「信頼できる団体」など、本サイト自身が収集・保証・評価していると誤認される表現を避ける。
- 未公開の分野、掲載候補、バックログ項目を、実装済み・公開済みとして記載しない。
- 支援・復旧分野の直接掲載先は行政の公式情報に限定し、金銭的支援先は熊本県公式の義援金案内だけとする。
- 支援分野の制限を、電気、ガス、通信、交通など公共性の高い民間事業者にまで拡張しない。
- 掲載しない団体を不適切・非公式・詐欺などと断定せず、第一版の掲載基準と継続確認可能な範囲による判断と説明する。

## 公式性の確認

公式サイトや公式SNSは、次の根拠のいずれかを人が確認する。

- 団体公式サイトから対象ページやSNSへリンクされている
- 国や自治体の公式SNS一覧に掲載されている
- 団体公式サイトにアカウント名やIDが明記されている
- 公式サイトのお知らせでSNS開設が案内されている
- 公的に確認できる組織関係がある

認証バッジ、フォロワー数、表示名、プロフィールの自己申告、公式らしい投稿内容、他ユーザーからの紹介だけを根拠にしない。対象URL、公式性の根拠、公式性確認日を記録する。

確認できない場合は「公式SNSが存在しない」と断定せず、「本サイトの掲載基準に基づき、現在掲載している公式SNSはありません」のように確認範囲を示す。

## 個人情報・センシティブ情報

- 利用者の個人情報を掲載情報として扱わない。
- 行方不明者、安否不明者、被災者、負傷者、患者、救助対象者、搬送先などの情報を掲載・転載・推測しない。
- 個人の氏名、写真、住所、状況、個人宅への配送依頼などを扱わない。
- 新サイト内にログイン、アカウント登録、利用者向け入力フォーム、検索内容の入力機能を無断で追加しない。
- 広告、アフィリエイト、協賛による掲載順位変更を無断で追加しない。
- 現在のproductionは運営者独自のアクセス解析、Cookie、localStorageを使用しない。文字サイズ設定として`sessionStorage`へ`standard`または`large`だけを保存する。実装と利用者向けプライバシー説明を常に一致させる。
- Google Analyticsは未実装であり、明示的な設計と利用者確認の前に解析タグを追加しない。Cookie、ブラウザ保存領域、外部送信、同意、日英の説明を設計してから実装する。

## データと重複管理

同じ団体、URL、公式アカウント、相談先を、ページや分野ごとに別データとして重複管理しない。複数の導線から同じ情報を参照できる設計とする。管理単位、coreとlocale、参照関係は[データモデル](docs/DATA_MODEL.md)に従う。

`data/`は本番用管理データ、`schemas/`は管理単位ごとの正式な構造制約です。日本語と英語は同じlocale Schemaを使い、実在管理データ、公開ゲート、公開生成、検証が実装済みです。明示依頼なしに団体、案内先、地域、データ項目、Schemaを増やさず、実在情報は人が公式性と案内目的を確認した後だけ追加します。`data/`、Schema、公開成果物、生成成果物を直接または推測で変更せず、正本・生成・検証の責務を維持します。検証回避のために共通Schemaを作らず、外部Schemaを参照しません。詳細は[データSchema実装](docs/DATA_SCHEMA_IMPLEMENTATION.md)に従います。

## UI・アクセシビリティ

- スマートフォン・タブレットを優先し、基本は1カラムとする。
- タップ領域を十分に確保し、横スクロール前提の表を避ける。
- 色やアイコンだけで意味を伝えない。
- 見出し階層を守り、各ページの`h1`は一つとする。
- キーボード操作、本文へのスキップリンク、ページ内アンカーを考慮する。
- JavaScriptへの過度な依存を避け、JavaScriptなしでも主要リンクと詳細を利用可能にする。
- ライト・ダーク双方を実装する場合は十分なコントラストを確保する。
- productionの公的機関・関係団体の案内先、問い合わせ先、運営者プロフィールは新しいタブで開き、`target="_blank"`と`rel="noopener noreferrer"`を設定する。移動先と目的が分かるリンク文言にする。
- previewの架空URLはリンク化しない。previewで許可する外部リンクは運営者プロフィールだけとし、productionの外部リンク方針と混在させない。
- 「こちら」「詳しくはこちら」だけの曖昧なリンク文言を使わない。
- PDFはリンク前にPDFであることを示す。
- 110番・119番を、誤操作しやすい大きな発信ボタンや自動発信リンクにしない。
- サイト内検索を無断で追加せず、第一版の一覧はブラウザのページ内検索を利用する。

## 文書と設計判断

- コード、掲載基準、運用方法を変更するときは、関連するREADME、設計文書、掲載方針、運用方針、更新履歴も更新する。
- 設計判断を変更する場合は、理由、影響範囲、再検討日または判断日を記録する。
- README、設計文書、掲載方針、運用方針、更新履歴の用語・数値・状態を一致させる。
- 詳細説明は`docs/`に置き、READMEや本ファイルへ大量に重複させない。
- 未対応事項の詳細は[公開後バックログ](docs/POST_LAUNCH_BACKLOG.md)へ集約し、READMEや設計文書へ重複記載しない。
- 文書は日本語で作成する。基本文書は既存の大文字・アンダースコア形式、ADRは4桁連番と小文字・ハイフン形式に合わせる。
- 言語別文面状態は`locale_status`を使用し、`translation_status`を使用しない。
- 内部確認履歴と内部専用項目をブラウザや公開成果物へ含めない。

## 作業完了前の確認

品質管理基盤の対象環境はNode.js `24.18.0`、npm `11.16.0`とする。依存関係は`npm ci`で再現し、通常の最終確認では`npm run check`を実行する。

- 指定されていない依存パッケージを独断で追加しない。
- `package-lock.json`を手作業で編集しない。
- テストから外部ネットワーク、AWS、実在する公式サイトへ接続しない。
- 本番用`data/`と`schemas/`を変更した場合は`npm run validate:data`を実行する。
- 明示依頼なしに団体、案内先、地域、データ項目、正式なitem Schemaを追加しない。
- 実在する団体、URL、災害情報は、公式性と案内目的を人が確認し、管理データ・公開ゲート・日英・検証を整合させた場合だけ追加する。
- 複数Schemaから参照する共通Schemaや外部Schema参照を無断で追加しない。
- `npm run format`は対象ファイルを書き換える。`npm run check`はファイルを書き換えない。
- 既存MarkdownのPrettierベースラインを検査回避目的で追加・更新しない。変更した文書は整形し、登録があれば削除する。

最低限、次を実行する。

```sh
git diff --check
git status -s
git diff --stat
git diff
npm run validate:data
npm run check
```

さらに、相対リンク、見出し階層、日付、決定記録の連番、用語と数値、実装済み・公開済み・未実装の区別を確認する。作業前後の差分を見比べ、依頼範囲外のファイルを変更していないことを確認する。

## Git操作

ユーザーの明示的な依頼なしに、次を行わない。

- commit
- push
- Pull Requestの作成
- 既存差分の破棄・上書き
- ブランチの作成・切り替え

作業結果は、変更ファイル、検証結果、未確定事項とともに報告し、ユーザーが差分を確認できる状態で停止する。
